type: object
properties:
  user_name:
    type: string
    description: User Name
    example: "user_name"
    nullable: true
  user_unique_id:
    type: string
    description: User Unique Id of user
    example: "user_unique_id"
    nullable: true
  is_guest:
    type: boolean
    description: If user is a guest user
    nullable: true
    example: false
  image_url:
    type: string
    description: User Image Url
    example: "image_url"
    nullable: true
