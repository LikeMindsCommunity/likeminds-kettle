get:
  summary: User Config

  operationId: userConfig

  tags:
    - User

  security:
    - bearerAuth: []

  parameters:
    - $ref: "../../components/parameters/headers/platformType.yaml"
    - $ref: "../../components/parameters/headers/platformCode.yaml"
    - $ref: "../../components/parameters/headers/versionCode.yaml"
    - $ref: "../../components/parameters/headers/apiKey.yaml"
    - name: ingest_your_communities
      in: query
      description: Ingest Your Communities
      required: true
      schema:
        type: boolean
        default: false

  responses:
    "200":
      description: OK
