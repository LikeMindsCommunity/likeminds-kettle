get:
    operationId: getUserToken
    description: 
      This API retrieves the user token.

    tags:
      - Login
    security:
      - bearerAuth: []

    parameters:
      - $ref: "../../components/parameters/headers/platformType.yaml"

    responses:
      '200':
        description: User token retrieved successfully
        content:
          application/json:
            schema:
              $ref: "../../components/schemas/responses/user_token.yaml"
