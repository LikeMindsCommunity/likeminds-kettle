get:
  operationId: fetchUserCreatedPendingPosts
  tags:
    - Feed - V1

  summary: Fetch User Created Pending Posts
  description: This API fetches all pending posts created by the user.

  security:
    - bearerAuth: []

  parameters:
    - $ref: "../../../../components/parameters/headers/platformCode.yaml"
    - $ref: "../../../../components/parameters/headers/versionCode.yaml"
    - $ref: "../../../../components/parameters/headers/acceptVersion.yaml"
    - $ref: "../../../../components/parameters/path/user_id.yaml"

  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../../components/schemas/responses/feed_post_pending_user_created.yaml"
