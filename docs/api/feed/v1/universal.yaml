get:
  summary: Fetch Universal Feed

  operationId: fetchUniversalFeed

  tags:
    - Feed - V1

  description: This API fetches Universal Feed.

  security:
    - bearerAuth: []

  parameters:
    - $ref: "../../../components/parameters/query/page.yaml"
    - $ref: "../../../components/parameters/query/feedType.yaml"
    - $ref: "../../../components/parameters/query/topicIds.yaml"
    - $ref: "../../../components/parameters/query/feedType.yaml"
    - $ref: "../../../components/parameters/query/post_ids.yaml"

  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/responses/feed_universal.yaml"
