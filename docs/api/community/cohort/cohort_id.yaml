delete:
  summary: Delete Community Cohort
  operationId: deleteCommunityCohort
  tags:
    - Community
  security:
    - bearerAuth: []
  parameters:
    - $ref: "../../../components/parameters/headers/apiKey.yaml"
  requestBody:
    required: true
    content:
      application/json:
        schema:
          type: object
          properties:
            cohort_id:
              type: integer
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/responses/community_cohort_access_delete.yaml"
