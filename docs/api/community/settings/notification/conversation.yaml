get:
  summary: Get Community Conversation Notification Settings
  operationId: getCommunityConversationNotificationSettings
  tags:
    - Community
  security:
    - bearerAuth: []
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../../components/schemas/responses/community_conversation_notifications.yaml"
            
put:
  summary: Update Community Conversation Notification Settings
  operationId: updateCommunityConversationNotificationSettings
  tags:
    - Community
  security:
    - bearerAuth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../../../components/schemas/requests/community_conversation_notifications.yaml"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../../components/schemas/responses/community_conversation_notifications.yaml"