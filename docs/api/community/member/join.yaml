put:
  summary: Join Community

  operationId: joinCommunity

  tags:
    - Community

  security:
    - bearerAuth: []

  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/requests/community_join.yaml"

  responses: {}
