put:
  summary: Update a poll option by ID

  operationId: updatePollOptionById

  tags:
    - Poll

  security:
    - bearerAuth: []

  parameters:
    - $ref: "../../components/parameters/headers/authToken.yaml"
    - name: poll_id
      in: path
      description: ID of the poll to be updated
      required: true
      schema:
        type: string

  requestBody:
    description: Updated poll option details
    required: true
    content:
      application/json:
        example:
          text: "poll option"

  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            type: object
            properties:
              success:
                type: boolean
