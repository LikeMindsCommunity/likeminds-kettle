post:
  summary: Create Direct Message Chatroom
  operationId: createDMChatroom
  tags:
    - Chatroom
  security:
    - bearerAuth: []
  requestBody:
    content:
      application/json:
        schema:
          $ref: "../../../components/schemas/requests/dm_create.yaml"
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/responses/chatroom_dm_create.yaml"
