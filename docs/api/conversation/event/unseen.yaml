get:
  summary: Get Unseen Event Count
  operationId: getUnseenEventCount
  tags:
    - Conversation
  security:
    - bearerAuth: []
  parameters:
    - $ref: "../../../components/parameters/headers/apiKey.yaml"
  responses:
    "200":
      description: OK
      content:
        application/json:
          schema:
            $ref: "../../../components/schemas/responses/conversation_unseen.yaml"
