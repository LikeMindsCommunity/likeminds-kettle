put:
  summary: Update Widget
  operationId: updateWidget
  tags:
    - Widget
  parameters:
    - $ref: "../components/parameters/headers/authToken.yaml"
    - name: widget_id
      in: path
      description: ID of the widget to update
      required: true
      schema:
        type: string
  security:
    - bearerAuth: []
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../components/schemas/requests/widget_update.yaml"
  responses:
    "200":
      description: OK
      content:
        application/json:
          example:
            success: true
            data: {}
